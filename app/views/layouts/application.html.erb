<!DOCTYPE html>
<html>
  <head>
    <title><%= Candidat.first.prenom + ' ' + Candidat.first.nom + " 2022" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@800&family=Poppins:wght@900&family=Raleway:wght@700&display=swap" rel="stylesheet">
    <%= favicon_link_tag asset_path('favicon.png') %>
  </head>

  <body class="bg-gray-900">
    <%= render 'shared/navbar' %>
    <% if current_page?(root_path) %>
      <%= render 'shared/hero' %>
    <% end %>
    <main class="container flex flex-col mx-auto mt-20">
      <%= yield %>
    </main>
  </body>
  <%= render 'shared/footer' %>
</html>
